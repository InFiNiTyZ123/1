<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Dashboard</title>

</head>
<body>
    <!-- Header -->
    <header>
        <h1>Dashboard</h1>
    </header>

    <!-- Main content with Sidebar and Welcome Message -->
    <main>
        <!-- Sidebar -->
        <nav>
            <ul>
                <li><a href="/employee">Employer</a></li>
                <li><a href="#">Profile</a></li>
                <li><a href="#">Settings</a></li>
                <li><a href="#">Logout</a></li>
            </ul>
        </nav>

    </main>

    <!-- Footer -->
    <footer>
        <p>Footer content here</p>
    </footer>

    <script>
      /*
        // Fetch the user address from the query params or fallback to 'Guest'
        const params = new URLSearchParams(window.location.search);
        const userAddress = params.get('address') || 'Guest';
*/
        // Display the user's address in the welcome message
       // document.getElementById('welcomeMessage').textContent = `Welcome, ${userAddress}`;
    </script>
</body>
</html>
